let selectedElement;

// function select(element) {
//     selectedElement = document.querySelector(element);
//   }


let f3 = {
      changeClass: {
        thing: e => (selected = document.querySelector(e)),
        thing2: "nahh",
      },
      app: { //nested feilds

        y: "on no its not me",
      },
};
